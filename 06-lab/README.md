# Lab 

## Objectives

Create a Terraform configuration that uses the `random` provider to generate a random number between 1 and 10. Then uses that random number to get that element from a list of strings. 

The result should be output called `random_string`.
